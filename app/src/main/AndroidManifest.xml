<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dexterlab.sahil.demo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fflogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Registration" />
        <activity android:name=".MasterGroup" />
        <activity android:name=".Login" />
        <activity android:name=".FinancialFitness" />
        <activity android:name=".AnswerActivity" />
        <activity android:name=".QuestionListActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".GridViewLayout">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".CardViewActivity" />
        <activity android:name=".StartingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FFILogin" />
        <activity android:name=".NewAnswer"></activity>
    </application>

</manifest>